# Minimal Prometheus config: scrape itself. Enough to demonstrate the
# Observer prometheus source with real PromQL against real series.
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
